# 76 — an indented code block, read in full

## Goal

> Every acceptance criterion in GitHub issue #76 holds against a running axiom, and
> `uv run pytest` is green — a model that indents its example four spaces instead of
> fencing it produces a block the user can read to the end of every line, set apart from
> the prose the way a fenced block is, while fenced blocks, lists, tables and every
> unrendered path stay exactly as they are today.
