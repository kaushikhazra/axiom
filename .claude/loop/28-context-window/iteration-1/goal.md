# 28-context-window

## Goal

> Implement GitHub issue #28 of `kaushikhazra/axiom` — size the model's context window to the smaller of the model's own max and 70% of the machine's available memory — as Python under `C:/Projects/axiom/src/`, finished when every one of the issue's 9 acceptance criteria is demonstrably met.
