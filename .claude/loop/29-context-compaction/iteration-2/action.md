# Action

Before anything else: check whether the GPU/model-worker has stabilized. `curl http://localhost:11434/api/version`, then a single trivial real chat call (not through compaction) as the real test — the daemon staying reachable didn't predict the last three crashes, so trust the trivial-call result, not just the daemon ping.

**If it's stable:** re-run the same six-turn scenario from cycle 1 (`AXIOM_DEBUG_MAX_CONTEXT` small, natural varied turns, teal in the first message) all the way through to the final recall question. This is the same scenario that already showed both fixes working mid-run — it needs to complete, not be redesigned. If it completes and answers correctly, that closes A and B per `observe.md`'s bar. Then move to B's harder form: a single compaction pass over many pairs carrying many distinct facts (not just one), to check the bulleted-extraction instruction holds up under real load, not just a two-topic case.

**If it's still unstable after one retry:** stop retrying. Add unit/mocked test coverage for the two fixes' mechanisms (the existing `tests/test_compaction.py` fakes are the right shape for this — a fake client whose `older` includes a `system`-role message, asserting it's carried forward rather than re-sent to `compact()`). This is supporting evidence only, per `observe.md` — it does not close A or B on its own — but it's useful, real progress that doesn't depend on the GPU, and keeps the regression net (22 passing now) honest as the code keeps changing. Report the infrastructure blocker plainly rather than let it look like the loop is stalled for no reason.

Do not start C or D this cycle unless A and B both get a complete live proof with time to spare before the fail-safe (19:06 IST) — unlikely given where cycle 1 ended, but check the clock before deciding.
