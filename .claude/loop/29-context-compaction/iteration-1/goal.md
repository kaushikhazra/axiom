# 29-context-compaction

## Goal

> Implement GitHub issue #29 of `kaushikhazra/axiom` — compact older conversation history before the context window fills, with an escalating fallback (10 pairs kept -> 5 -> 2 -> 0) when a normal pass isn't enough — as Python under `C:/Projects/axiom/src/`, finished when every one of the issue's 11 acceptance criteria is demonstrably met.
