# 77 — the answer, told apart from everything said around it

## Goal

> Every acceptance criterion in GitHub issue #77 holds against a running axiom, and
> `uv run pytest` is green — a user sees the model list and the session's facts as
> bordered panels in one accent colour, reads a reply whose structure carries that same
> accent, watches a turn call tools without a single line per call, and finds everything
> axiom says about itself drawn quieter than the answer it was asked for.
